import React, { useState } from 'react';
import { Home, Users, Briefcase, Bell, User, Search, Heart, MessageCircle, ChevronRight, LogOut, Languages } from 'lucide-react';

export default function App() {
  const [tab, setTab] = useState('home');

  return (
    <div className="min-h-screen bg-gray-50 text-gray-800 pb-20 font-sans max-w-md mx-auto shadow-lg relative">
      <header className="bg-emerald-700 text-white p-4 sticky top-0 z-50 flex justify-between items-center shadow">
        <div>
          <h1 className="font-bold text-lg">ইরাক প্রবাসী হেল্প</h1>
          <p className="text-[10px] text-emerald-100">مساعدة للمغتربين في العراق</p>
        </div>
        <Bell className="w-5 h-5 cursor-pointer" />
      </header>

      <main className="p-4 space-y-4">
        {tab === 'home' && (
          <>
            <div className="bg-white p-4 rounded-2xl shadow-sm border border-gray-100 flex justify-between items-center">
              <div>
                <p className="text-xs text-gray-400">বাগদাদ, ইরাক</p>
                <h2 className="text-md font-bold text-gray-900">স্বাগত, মোস্তফা</h2>
              </div>
              <div className="w-10 h-10 bg-emerald-100 text-emerald-700 rounded-full flex items-center justify-center font-bold">মু</div>
            </div>

            <div className="bg-gradient-to-r from-emerald-600 to-teal-700 text-white p-4 rounded-2xl shadow">
              <span className="bg-emerald-800 text-[10px] px-2 py-0.5 rounded-full">২৪/৭ সহায়তা</span>
              <h3 className="text-lg font-bold mt-2">প্রবাসীর সেবা আমাদের অঙ্গীকার</h3>
            </div>

            <div className="grid grid-cols-3 gap-3">
              {[
                { title: 'চাকরি', icon: Briefcase, id: 'jobs' },
                { title: 'নোটিশ', icon: Bell, id: 'notices' },
                { title: 'প্রোফাইল', icon: User, id: 'profile' }
              ].map((item, i) => (
                <button key={i} onClick={() => setTab(item.id)} className="bg-white p-3 rounded-2xl border border-gray-100 flex flex-col items-center justify-center shadow-sm">
                  <item.icon className="w-5 h-5 text-emerald-600 mb-1" />
                  <span className="text-xs font-medium">{item.title}</span>
                </button>
              ))}
            </div>
          </>
        )}

        {tab === 'jobs' && (
          <div className="space-y-3">
            <h3 className="font-bold text-gray-800 text-sm">চাকরির সুযোগ</h3>
            <div className="bg-white p-3 rounded-2xl border border-gray-100 shadow-sm">
              <h4 className="font-bold text-sm text-gray-900">হেভী ড্রাইভার • বাগদাদ</h4>
              <p className="text-xs text-emerald-700 font-semibold mt-1">IQD 750,000</p>
            </div>
          </div>
        )}

        {tab === 'notices' && (
          <div className="space-y-3">
            <h3 className="font-bold text-gray-800 text-sm">নোটিশ / বিজ্ঞপ্তি</h3>
            <div className="bg-white p-3 rounded-2xl border border-gray-100 shadow-sm">
              <h4 className="text-xs font-bold text-gray-900">পাসপোর্ট নবায়ন সংক্রান্ত গুরুত্বপূর্ণ নোটিশ</h4>
              <p className="text-[10px] text-gray-400 mt-1">০৫ মে, ২০২৬ • জরুরী</p>
            </div>
          </div>
        )}

        {tab === 'profile' && (
          <div className="space-y-3">
            <div className="bg-white p-4 rounded-2xl text-center border border-gray-100 shadow-sm">
              <h3 className="font-bold text-gray-900 text-sm">মুস্তফা বাংলাদেশ</h3>
              <p className="text-xs text-gray-500">বাগদাদ, ইরাক</p>
            </div>
            <div className="bg-white rounded-2xl border border-gray-100 overflow-hidden divide-y divide-gray-100 text-xs">
              <div className="p-3 flex justify-between items-center text-red-600"><span className="flex items-center space-x-2"><LogOut className="w-4 h-4" /><span>লগ আউট</span></span></div>
            </div>
          </div>
        )}
      </main>

      <nav className="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 py-2 px-4 flex justify-around items-center max-w-md mx-auto z-50">
        {[
          { id: 'home', title: 'হোম', icon: Home },
          { id: 'jobs', title: 'চাকরি', icon: Briefcase },
          { id: 'notices', title: 'নোটিশ', icon: Bell },
          { id: 'profile', title: 'প্রোফাইল', icon: User }
        ].map((t) => (
          <button key={t.id} onClick={() => setTab(t.id)} className={`flex flex-col items-center ${tab === t.id ? 'text-emerald-600 font-bold' : 'text-gray-400'}`}>
            <t.icon className="w-5 h-5" />
            <span className="text-[10px]">{t.title}</span>
          </button>
        ))}
      </nav>
    </div>
  );
}
